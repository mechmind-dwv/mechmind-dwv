name: "📜 MechMind Documentation Engine"

on:
  push:
    branches: [ "main" ]
    paths:
      - 'docs/**'
      - 'projects/**'
  workflow_dispatch:

env:
  ROS_DISTRO: humble

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  documentation:
    name: "📚 Generar Documentación"
    runs-on: ubuntu-22.04
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: "🛸 Checkout del Código"
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup ROS2 Humble
        uses: ros-tooling/setup-ros@v0.7
        with:
          required-ros-distributions: humble

      - name: "⚙️ Configurar Rust Nightly"
        uses: dtolnay/rust-toolchain@nightly
        with:
          components: rust-docs

      - name: "🦀 Generar RustDoc"
        run: |
          source /opt/ros/humble/setup.bash
          cargo +nightly doc --no-deps --all-features --workspace
          mkdir -p public
          cp -r target/doc/* public/
          if [ ! -f public/index.html ]; then
            echo '<meta http-equiv="refresh" content="0; url=mechros2/index.html">' > public/index.html
          fi

      - name: "📤 Upload Artifact"
        uses: actions/upload-pages-artifact@v3
        with:
          path: public

      - name: "🚀 Desplegar en GitHub Pages"
        id: deployment
        uses: actions/deploy-pages@v4
