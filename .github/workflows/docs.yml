## ⚡ Nuestro Workflow Profesional
name: 🛠️ MechMind Docs CI
on:
  push:
    branches:
      - main
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Instala dependencias y genera docs
        run: |
          # Aquí tu comando para instalar dependencias y generar documentación
          echo "Generando documentación..."
      - name: Publica en GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          publish-dir: docs/robotics/api/rustdoc
